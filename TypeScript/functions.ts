function addTwo(num: number): number{
    return num + 2;
}
addTwo(10);

const getHello = (s: string): string => {
    return `Hello ${s}`;
}

const heros = ["thor", "spiderman", "ironman"]

heros.map(hero => {
    return `hero is ${hero}`
})

